/* typeChecking.h */

#include <iostream>
#include <regex>
#include <string>
using namespace std;

bool isInt(string);
bool isFloat(string);
bool isOperator(string);
bool isVariable(string);
bool isKeyword(string);
