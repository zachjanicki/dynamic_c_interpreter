// Some type checking functions...

#include <iostream>
#include <regex>
#include "token.h"

bool isInt(token);
bool isFloat(token);
bool isOperator(token);
bool isVariable(token);
bool isKeyword(token);
